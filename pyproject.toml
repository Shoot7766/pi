[project]
name = "pi"
version = "0.1.0"
dependencies = [
    "crewai>=0.28.0",
]

[tool.hatch.build.targets.wheel]
packages = [
    "src/pi",
    "agents",
    "config",
    "core",
    "tools",
    "interfaces"
]
